import { symTrees } from "./premadeTrees.mjs";

export const isSymmetric = (root) => {
    // your code here
  };

/*
  console.log(isSymmetric({
    value: 1,
    left: { value: 2, left: { value: 3 }, right: { value: 4 } },
    right: { value: 2, left: { value: 4 }, right: { value: 3 } }
  }));
  // true, the tree is symmetric

  console.log(isSymmetric({
    value: 1,
    left: { value: 2, right: { value: 3 } },
    right: { value: 2, right: { value: 3 } }
  }));
  // false, the tree is not symmetric

  console.log(isSymmetric(null));
  // true, an empty tree is considered symmetric

  console.log(isSymmetric({
    value: 1,
    left: { value: 2, left: { value: 3 } },
    right: { value: 2, left: { value: 3 } }
  }));
  // false, one side has left children, the other doesn't

  console.log(isSymmetric({
    value: 1
  }));
  // true, a single-node tree is symmetric
  
*/
